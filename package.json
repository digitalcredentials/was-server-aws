{
  "name": "was-server-aws",
  "version": "1.0.0",
  "description": "Wallet Attached Storage server on AWS SAM",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm --prefix events test"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.600.0"
  }
}
